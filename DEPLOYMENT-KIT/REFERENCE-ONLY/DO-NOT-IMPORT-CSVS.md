# Reference Only

The CSV files in this folder are readable snapshots of the final proposed Sheet rows. Do not import them into Google Sheets. The guarded Apps Script in `GOOGLE-SHEETS` is the supported application method because it preserves existing formatting and checks for newer edits.
